import React from 'react';

export default (props) => {
    return (
            <div>
                <p> My hobbies are playing chess, working out, mixed martial arts, reading books and writing</p>
            </div>
    );
};
